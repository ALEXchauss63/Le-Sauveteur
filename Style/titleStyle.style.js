import { StyleSheet } from "react-native";
import { primaryColor } from "./utils/color";

const titletyle = StyleSheet.create({
  title: {
    color: primaryColor,
    textAlign: "center",
    fontSize: 24,
    marginVertical: 50,
  },
});
export default titletyle;
