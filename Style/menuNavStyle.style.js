import { StyleSheet } from "react-native";

const menuNavStyle = StyleSheet.create({
  container: {
    zIndex: 1,
    flexDirection: "row",
    position: "absolute",
    bottom: 25,
    left: 45,
  },
  sos: {
    marginHorizontal: 25,
    bottom: 100,
  },
});
export default menuNavStyle;
