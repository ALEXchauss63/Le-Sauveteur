import { StyleSheet } from "react-native";

const menuNavStyle = StyleSheet.create({
  container: {
    zIndex: 1,
    flexDirection: "row",
    alignItems: "center",
    position: "absolute",
    bottom: 10,
    left: 30,
  },
  sos: {
    marginHorizontal: 15,
  },
});
export default menuNavStyle;
